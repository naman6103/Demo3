pipeline {
    agent {
        node {
            label 'Built-In Node'
        }
    }
    stages {
        stage('Test') {
            steps {
                echo 'Testing...'
                sh 'mvn clean install -DskipTests'
            }
        }
    }
}