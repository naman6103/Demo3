pipeline {
    agent {
        docker {
            image 'maven'
            args '-u root'
        }
    }
    stages {
        stage('Test') {
            steps {
                echo 'Testing...'
                sh 'mvn clean install -DskipTests'
            }
        }
    }
}