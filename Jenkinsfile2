pipeline {
    agent {
        node {
            label 'maven'
        }
    }
    stages {
        stage('Test') {
            steps {
                echo 'Testing...'
                sh 'mvn clean install -DskipTests'
            }
        }
    }
}