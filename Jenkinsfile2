pipeline {
    agent {
        docker {
            image 'node'
            args '-u root'
        }
    }
    stages {
        stage('Test') {
            steps {
                echo 'Testing...'
                sh 'maven clean install -DskipTests'
            }
        }
    }
}