pipeline {
    agent {
        node {
            label 'build-in'
        }
    }
    stages {
        stage('Test') {
            steps {
                echo 'Testing...'
                sh 'mvn clean install -DskipTests'
            }
        }
    }
}